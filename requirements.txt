# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12    # needed for file uploads (Whisper)
pydantic==2.9.0
pydantic-settings==2.5.2

# Groq — LLM + Whisper
groq==0.11.0
httpx==0.27.2

# HuggingFace — local emotion model
transformers==4.44.2
torch==2.4.1                # CPU build is fine; use torch+cu121 for GPU
# onnxruntime==1.19.2       # optional: swap transformers for 2x faster CPU inference

# Logging / utilities
python-dotenv==1.0.1

mlflow